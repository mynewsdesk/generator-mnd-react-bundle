module.exports = function <%= bundleName %>Endpoint(app) {
  // app is an express application, just mount your Endpoint
  // ie:
  /*
    app.post('/<%= bundleName %>/create', (req, res) => {
      res.send('Entity successfuly created');
    });
   */
};
